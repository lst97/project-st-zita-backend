export const API_VERSION = "v1";
export const PORT = 1167;
export const HOST = "localhost";
export const API_ENDPOINTS = `/api/${API_VERSION}`;
export const SERVER_URL = `http://${HOST}:${PORT}${API_ENDPOINTS}`;
